<UserControl
    x:Class="CrossInputShare.UI.Controls.DeviceCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CrossInputShare.UI.Converters"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="300">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border
        Background="{ThemeResource CardBackgroundFillColorSecondary}"
        BorderBrush="{ThemeResource CardStrokeColorDefault}"
        BorderThickness="1"
        CornerRadius="6"
        Padding="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!-- Status Indicator -->
            <Border 
                Grid.Column="0"
                Grid.RowSpan="2"
                Width="12"
                Height="12"
                CornerRadius="6"
                Background="{Binding StatusColor}"
                Margin="0,0,12,0"
                VerticalAlignment="Center" />
            
            <!-- Device Name and Role -->
            <StackPanel Grid.Column="1" Grid.Row="0">
                <TextBlock 
                    Text="{Binding DisplayName}" 
                    FontSize="14"
                    FontWeight="SemiBold"
                    VerticalAlignment="Center" />
                <TextBlock 
                    Text="{Binding RoleDisplay}" 
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondary}"
                    VerticalAlignment="Center" />
            </StackPanel>
            
            <!-- Platform -->
            <TextBlock 
                Grid.Column="1" 
                Grid.Row="1"
                Text="{Binding Platform}"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondary}"
                Margin="0,4,0,0" />
            
            <!-- Connection Time -->
            <TextBlock 
                Grid.Column="2" 
                Grid.Row="0"
                Text="{Binding ConnectedSince, StringFormat='Connected: {0:HH:mm}'}"
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondary}"
                HorizontalAlignment="Right" />
            
            <!-- Feature Toggles -->
            <StackPanel 
                Grid.Column="0" 
                Grid.ColumnSpan="3"
                Grid.Row="2"
                Orientation="Horizontal"
                Spacing="8"
                Margin="0,12,0,0">
                <ToggleButton 
                    Content="⌨️"
                    ToolTipService.ToolTip="Keyboard"
                    IsChecked="{Binding IsKeyboardShared, Mode=TwoWay}"
                    Command="{Binding ToggleKeyboardCommand}"
                    Width="40"
                    Height="32"
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="1"
                    CornerRadius="4" />
                <ToggleButton 
                    Content="🖱️"
                    ToolTipService.ToolTip="Mouse"
                    IsChecked="{Binding IsMouseShared, Mode=TwoWay}"
                    Command="{Binding ToggleMouseCommand}"
                    Width="40"
                    Height="32"
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="1"
                    CornerRadius="4" />
                <ToggleButton 
                    Content="📋"
                    ToolTipService.ToolTip="Clipboard"
                    IsChecked="{Binding IsClipboardShared, Mode=TwoWay}"
                    Command="{Binding ToggleClipboardCommand}"
                    Width="40"
                    Height="32"
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="1"
                    CornerRadius="4" />
                <ToggleButton 
                    Content="🖥️"
                    ToolTipService.ToolTip="Screen"
                    IsChecked="{Binding IsScreenShared, Mode=TwoWay}"
                    Command="{Binding ToggleScreenCommand}"
                    Width="40"
                    Height="32"
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                    BorderThickness="1"
                    CornerRadius="4" />
            </StackPanel>
            
            <!-- Local Device Badge -->
            <Border 
                Grid.Column="2"
                Grid.Row="1"
                Background="#2196F3"
                CornerRadius="4"
                Padding="4,2"
                HorizontalAlignment="Right"
                Visibility="{Binding IsLocalDevice, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock 
                    Text="This Device"
                    FontSize="10"
                    Foreground="White"
                    FontWeight="SemiBold" />
            </Border>
        </Grid>
    </Border>
</UserControl>